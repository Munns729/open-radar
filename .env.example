# =============================================================================
# RADAR Environment Configuration
# Copy to .env and fill in your API keys.
# =============================================================================

# --- Environment ---
ENVIRONMENT=development
LOG_LEVEL=INFO

# =============================================================================
# REQUIRED (core functionality)
# =============================================================================

# --- Database (required) ---
# Run: docker-compose up -d
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/radar

# --- LLM: Analysis (moat scoring, semantic enrichment) ---
# Kimi K2.5 — strong model required. Get key at https://platform.moonshot.ai/
MOONSHOT_API_KEY=
KIMI_API_BASE=https://api.moonshot.ai/v1
KIMI_MODEL=kimi-k2.5

# --- LLM: Browsing agents (website discovery, extraction) ---
# Use local Ollama for cost/speed. Run: ollama pull qwen3:8b
OPENAI_API_KEY=
OPENAI_API_BASE=http://localhost:11434/v1
BROWSING_MODEL=qwen3:8b
LLM_REQUEST_TIMEOUT=180

# Hybrid: MOONSHOT_API_KEY for analysis + OPENAI_API_BASE for browsing (Ollama)

# =============================================================================
# DATA SOURCES (enable features)
# =============================================================================

# --- Companies House (UK company data) ---
# Free: https://developer.company-information.service.gov.uk/
# Enables: UK company financials, officer data, filings
COMPANIES_HOUSE_API_KEY=

# --- FCA Register (UK PE/VC firm discovery) ---
# Free: https://register.fca.org.uk/Developer/s/
# Enables: Capital flows scan --sources FCA
FCA_API_EMAIL=
FCA_API_KEY=

# =============================================================================
# OPTIONAL (enhanced features)
# =============================================================================

# --- Anthropic Claude (semantic enrichment) ---
# Semantic enrichment prefers Claude when available. Fallback: Kimi.
# https://console.anthropic.com/
ANTHROPIC_API_KEY=

# --- Currency ---
BASE_CURRENCY=GBP
PREFERRED_CURRENCY=GBP
# CURRENCY_DATE=2025-01-15  # Pin date, or leave blank for dynamic

# --- Web UI Auth (optional) ---
# Basic auth for /api. Default: admin / radar
# RADAR_USERNAME=admin
# RADAR_PASSWORD=radar

# --- Thesis Config (optional) ---
# Override path to thesis.yaml. Default: config/thesis.yaml
# THESIS_CONFIG_PATH=config/thesis.yaml

# --- SSL (optional) ---
# Set to 1 to bypass cert verification for scrapers (BSI bund.de, broken company sites)
# IGNORE_SSL_ERRORS=0

# =============================================================================
# NOTIFICATIONS (optional)
# =============================================================================

# --- SendGrid (email alerts) ---
# SENDGRID_API_KEY=
# SENDGRID_FROM_EMAIL=radar@yourdomain.com
# ADMIN_EMAIL=you@yourdomain.com

# --- Slack (alert webhooks) ---
# SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx/yyy/zzz

# =============================================================================
# INTEGRATIONS (optional)
# =============================================================================

# --- Notion ---
# NOTION_API_KEY=

# --- Gmail (relationships / contact sync) ---
# Path to Google OAuth credentials JSON
# GMAIL_CREDENTIALS_PATH=/path/to/credentials.json

# --- Ngrok (public access / remote dev) ---
# For run_public.py. Get token at https://dashboard.ngrok.com/
# NGROK_AUTH_TOKEN=
